# This script contains git command to stage, commit and push the changes to the 'main' branch of the remote repository.

# Stage the changes
git add .

# Commit the changes
git commit -m "Modified content"

# Push the changes to the 'main' branch of the remote repository
git push origin main
